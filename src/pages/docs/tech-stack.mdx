---
title: BlogCard
description: BlogCard's UI for your blog or news website.
---

import Image from "next/image"
import { Tab, Tabs } from "nextra-theme-docs"

# Tech Stack

## Ecosystem

Campusorbit is not just a web app. It's an entire ecosystem ecosystem containing three divisions:

- Campusorbit Website
- Campusorbit Android application
- Backend written entirely in Rust

## Technologies Used:

### Website

<div className="flex flex-wrap gap-4">
  <div>
    <Image src="/tech/react.svg" alt="" height={75} width={75} />
    <p className="text-center">ReactJs</p>
  </div>
  <div>
    <Image src="/tech/nextjs.svg" alt="" height={75} width={75} />
    <p className="text-center">NextJs</p>
  </div>
</div>

### Android app

<div className="flex flex-wrap gap-4">
  <div>
    <Image src="/tech/flutter.svg" alt="" height={75} width={75} />
    <p className="text-center">ReactJs</p>
  </div>
  <div>
    <Image src="/tech/dart.svg" alt="" height={75} width={75} />
    <p className="text-center">NextJs</p>
  </div>
</div>

### Backend

- Framework: Nextjs
- UI: Tailwindcss along with Theming

## Requirement

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

```sh copy
npm i @tailwindcss/aspect-ratio
```

```js filename="tailwind.config.js" copy
/** @type {import('tailwindcss').Config} */
module.exports = {
	...,
	theme: {
		extend: {
			height: {
				100: "30rem",
			},
			width:{
				90:"22rem",
			}
		},
	},
	plugins: [require("@tailwindcss/aspect-ratio")],
};
```

Extend `height` with new value and install plugins to work with image.
